# Core ADK with A2A support - specific versions for MCP compatibility
google-adk[a2a]==1.16.0

# MCP library - specific version for compatibility
mcp==1.12.0

# Pydantic and FastAPI - specific versions to avoid serialization issues
pydantic==2.10.6
fastapi==0.118.3

# Authentication
google-auth>=2.0.0

# Environment and configuration
python-dotenv>=1.0.0

# Web framework (for running the agent as a service)
uvicorn>=0.34.0